//Use Multiple Conditional (Ternary) Operators

/*In the checkSign function, use multiple conditional operators. The function shld return
positive, negative or zero. */

function checkSign(num) {
    return (num > 0) ? "positive"
    : (num < 0) ? "negative"
    : "zero";
}

checkSign(10);